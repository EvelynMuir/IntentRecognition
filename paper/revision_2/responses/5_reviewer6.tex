% ================================
% === Reviewer 6 ===
% ================================
\reviewerblock{Reviewer~6}{%
We thank the reviewer for the positive assessment of the writing and structure, and for the precise methodological requirement, which we have implemented in full.}

\cresp{The paper presents a functionally decoupled framework for visual-intent recognition that addresses supervisory and semantic ambiguity using structured semantic priors and uncertainty-aware distillation. The manuscript is well written, clearly structured, and the proposed contribution is reasonably good and relevant to the field. However, the performance improvement over comparable state-of-the-art methods is relatively small, which limits the strength of the empirical contribution. Although the authors report results using mean $\pm$ standard deviation over three runs, this alone is not sufficiently convincing for establishing statistical superiority. An explicit paired t-test, applied consistently against the strongest state-of-the-art methods, should therefore be provided to demonstrate that the observed improvements are statistically significant rather than caused by random variation.}
{\gist{We implemented the requested test, namely five seeds with paired two-sided $t$-tests against every matched-protocol comparator, Holm-corrected, with the full statistics in Table~\ref{tab:significance_letter} of this letter. We additionally strengthened the empirical contribution itself, with a cross-dataset study and an extended comparison against two 2026 methods and four recent multimodal large models.}

We agree that mean$\pm$std over three runs does not establish statistical superiority.

\rh{Protocol.} We extend from three to \res{five} independent seeds. For each seed we retrain, on that same seed, the matched-protocol reproductions of the three strongest comparable methods, namely HLEG, LabCR, and IntCLIP, over identical frozen CLIP ViT-L/14 features, the identical split, and identical validation-only class-wise thresholding. This gives five \emph{paired} observations per method, with the training run as the pairing unit, so a paired two-sided $t$-test on the per-seed differences is well defined. Since FDIL is tested against five methods per metric, we also report Holm-corrected $p$-values.

\rh{Outcome.} Avg.\ F1, mAP, and hard-subset F1 are significant against every matched-protocol comparator after correction, the published-only rows being quoted from their papers and hence outside the paired design. Against the strongest compared method, LabCR, the gains are \res{$+3.32$} Avg.\ F1 and \res{$+7.67$} hard-subset F1. The class-wise-thresholded F1 metrics carry more seed-to-seed variance because thresholds are fitted on a $498$-image validation split, and the only non-significant contrast is Samples F1 against our own UTD-only ablation ($p_{\mathrm H}=0.0755$), which we report alongside the significant ones. The gains over the matched published methods are therefore statistically reliable, while remaining consistent with the modest effect size the reviewer correctly identified. Sec.~\ref{sec:sota_comparison} states the outcome and protocol, and the full statistics are provided in Table~\ref{tab:significance_letter}.

\rh{Beyond the requested test.} Because the reviewer's concern is ultimately about the strength of the empirical contribution and not only about its statistical treatment, we have also broadened the evidence in two ways. First, the evaluation is no longer confined to a single benchmark: the new Sec.~\ref{sec:crossdataset} adds a cross-dataset study, comprising a controlled $5\times5$ cross-validation on the larger EMOTIC benchmark, where every aggregate metric improves significantly over the matched baseline, and a transfer to two label-distribution benchmarks, Flickr-LDL and Emotion6, where the class-wise threshold search disappears entirely and the division of labour between the two modules nevertheless replicates. Second, the comparison set is extended with two 2026 intent-recognition methods, CoT4Intent and HVU-CLIP~(TPAMI 2026), the latter also under a same-backbone ResNet101 comparison, together with four recent multimodal large models, Qwen3.5-9B, Step3-VL-10B, Gemma-4-E4B-IT, and GPT-5.6~Sol, in a dedicated zero-shot table. Together with the paired tests above, this places the reported improvements against both stronger baselines and unseen datasets rather than against seed variation alone.
\revloc{Sec.~\ref{sec:sota_comparison}, the confidence intervals and regenerated matched-protocol rows in Table~\ref{tab:main_results} whose caption now states five seeds, the new Sec.~\ref{sec:crossdataset} with Tables~\ref{tab:emotic_pool} and~\ref{tab:ldl_transfer}, the new rows in Tables~\ref{tab:main_results}, \ref{tab:backbone_ablation}, and~\ref{tab:zeroshot_vlm}, and Table~\ref{tab:significance_letter} of this letter}
}
